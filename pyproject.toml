# {{{ ////////////////////////////////////////////////////////////////

# ============================================================
# Build system (PEP 518 / PEP 517)
# ============================================================
[build-system]

# --- [hatch] ---------------------------------------------------

requires = ["hatchling>=1.30.1"]
build-backend = "hatchling.build"

# --- [uv_build] ---------------------------------------------------

#requires = ["uv_build>=0.11.26,<0.12"]
#build-backend = "uv_build"


# ============================================================
# Project metadata (PEP 621)
# ============================================================
[project]
name = "dve_sample_py"
version = "0.1.0"
description = "TODO(title): TITLE OF THE RESEARCH WORK / TITOLO DEL LAVORO DI RICERCA"
readme = "README.md"
license-files = [
     "LICENSES/*.txt",
     "LICENSE.md",
]
requires-python = ">=3.14,<3.15"

authors = [
  { name = "Data Science Lab DEMS/datalab", email = "datalab@unimib.it" },
]

# }}} //
# {{{ ////////////////////////////////////////////////////////////////


# ============================================================
# Dependencies (PEP 508 strings)
# ============================================================
dependencies = [

  # --------------------------------------------------------------------
  # --- ML frameworks ---
  # --------------------------------------------------------------------
  #
  # moved to [project.optional-dependencies]
  # to support both 'gpu' and 'cpu' architectures
  #
  # usage:
  #
  #   uv lock
  #
  #   uv sync --all-groups --extra=gpu  
  # or 
  #   uv sync --all-groups --extra=cpu
  #
  #
  # to detect GPU, in shell:
  #
  # (type nvidia-smi && nvidia-smi -L) &> /dev/null \
  #   && HAS_GPU=1 || HAS_GPU=0; echo $HAS_GPU
  #
  # see:
  # - https://docs.astral.sh/uv/guides/integration/pytorch/
  # - https://zenn.dev/haru256/articles/6ded722b409d13?locale=en
  # - AGENTS.md
  # --------------------------------------------------------------------
  

  # --- data ---
  "numpy",
  "pandas",
  "pandas-datareader",
  "openpyxl",
  # "h5py",
  "pyarrow",
  "polars",

  # --- sql ---
  "SQLAlchemy",
  "psycopg2-binary",
  "mysql-connector-python",
  # "mysqlclient",
  
  # --- db ---
  "duckdb",

  # --- stats ---
  "scipy",
  "sympy",

  # --- plots ---
  "matplotlib",
  "plotly",
  "seaborn",
  "Pillow",

  # --- graphs ---
  "graphframes",
  "graphviz",
  "networkx",
  "igraph",
  "pyvis",
  "pydot",

  # --- system ---
  "requests",
  "oauthlib",
  "requests-oauthlib",
  "urllib3",
  "aiohttp",
  "pyzmq",

  "pyyaml",
  "types-pyyaml",
  "toml",
  "types-toml",
  "piny",
  "click",
  "pydantic",
  "pathspec",

  "icecream",
  #"radian",
  "tqdm",

  "pip",
  "packaging",
  "more-itertools",

  # --- git dependency ---
  "rootpath @ git+https://github.com/hute37/python-rootpath@stable",

]


# ============================================================
# Optional dependency groups (PEP 621 Extras)
# ============================================================
[project.optional-dependencies]

cpu = [

  "torch==2.10.*",
  "torchvision==0.25.*",

]

gpu = [

  "torch==2.10.*",
  "torchvision==0.25.*",

  ### "tensorflow==2.21.*",
  ### "tensorflow-datasets==4.9.*",
  ### "tensorboard",

  
  ## "pynvml; sys_platform == 'linux'",
  "nvidia-ml-py; sys_platform == 'linux'",

  # Index source: controlled by [[tool.uv.index]] name = "torch-gpu".
  # In GPU environments (dev / test / prod) the index resolves to:
  # https:^//download.pytorch.org/whl/cu130
  # In CI set:
  # UV_INDEX_TORCH_GPU=https:^//download.pytorch.org/whl/cpu
  # to redirect to the CPU wheel server, eliminating triton and all
  # CUDA-bundled libraries from the virtual environment  # "tensorflow==2.20.*",

  
]


# ============================================================
# Project URLs
# ============================================================

[project.urls]
documentation = "https://gitlab.com/ub-dems-public/ds-labs/dve-sample-py/-/wikis/home"
homepage = "https://gitlab.com/ub-dems-public/ds-labs/dve-sample-py"
repository = "https://gitlab.com/ub-dems-public/ds-labs/dve-sample-py.git"

# ============================================================
# Script commands
# ============================================================

[project.scripts]
main = "dve.cli:main"
demo = "vce.cli:main"
hello = "dve.scripts.dummy.dummy_hello:main"


# ============================================================
# Development dependency groups
# ============================================================
[dependency-groups]

agents = [

  # --- skills ---
  # "anthropic",
  # "openai",
  # "graphifyy",
  # "scikit-learn",
  # "sentence-transformers",

]

jupyter = [

  # --- jupyter ---
  "jupyter",
  "jupyter-core",
  "jupyter-console",
  "jupyter-lsp",
  "notebook",
  "jupyterlab",
  "jupytext",

  "nodeenv",
  "nodejs",

  "jupyterlab-lsp",
  "jupyter-ruff",

  # "pyright",
  # "python-lsp-server[yapf,rope,pyflakes]",
  # "python-lsp-black",
  # "black[jupyter]",

  "lux-api",

  "ipykernel",
  "ipywidgets",
  "ipython",
  "ipython-bg",

  # --- marimo ---

  "marimo",
]

dev = [

  # --- test ---
  "pytest",
  "pytest-cov",
  "hypothesis",
  "behave",
  "mock",
  "nose",

  # --- profile ---
  "line-profiler",
  "memory-profiler",

  # --- edit ---
  "pyright",
  "basedpyright",
  "tree-sitter",
  "semgrep",
  "mypy",

  # --- debug ---
  "debugpy",

  # --- lint ---
  "ruff",
  # "yapf",

  # --- license ---
  "reuse",

  # --- docs ---
  # "sphinx",
  # "nbsphinx",
  # "sphinx-autoapi",
  # "sphinx-rtd-theme",

  # --- data ---
  "csvkit",

  # --- misc ---
  "colorama",
  #"py",

  # --- packaging ---
  "setuptools",
  "wheel",
  "bumpversion",
  "twine",
]


# }}} //
# {{{ ////////////////////////////////////////////////////////////////

# ============================================================
# Tool configuration (non-standard, tool-specific tables)
# ============================================================

# ===[ENV]=================================================================
  
# --- [uv] ---------------------------------------------------

[tool.uv]
# Treat this directory as an installable package
package = true

# Prevent both "cpu" and "gpu" from being specified at the same time
conflicts = [
  [
    { extra = "cpu" },
    { extra = "gpu" },
  ],
]

[tool.uv.sources]
packaging = { index = "pypi" }

torch = [
  { index = "pytorch-cpu", extra = "cpu" },
  { index = "pytorch-gpu", extra = "gpu" },
]
torchvision = [
  { index = "pytorch-cpu", extra = "cpu" },
  { index = "pytorch-gpu", extra = "gpu" },
]

### [tool.uv.environment]
### # For CI: Force CPU-only
### UV_INDEX_TORCH_GPU = { value = "https://download.pytorch.org/whl/cpu", if = "'CI' in os.environ" }
### UV_EXTRA_INDEX_URL = { value = "https://download.pytorch.org/whl/cpu", if = "'CI' in os.environ" }

[[tool.uv.index]]
name = "pypi"
url = " https://pypi.org/simple"
explicit = false


[[tool.uv.index]]
# GPU default: CUDA 13.0-compiled wheels (driver ^>= 580).
# Rename from "torch130" to "torch-gpu" so the uv env-var override reads:
# UV_INDEX_TORCH_GPU=<url>
name = "pytorch-gpu"
url = "https://download.pytorch.org/whl/cu130"
explicit = true

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

  
# # --- [poetry] ---------------------------------------------------

# [tool.poetry]
# requires-poetry = ">=2.0"
# packages = [{ include = "*", from = "src" }]

# [[tool.poetry.source]]
# name = "torch130"
# url = "https://download.pytorch.org/whl/cu130"
# priority = "explicit"

# [tool.poetry.dependencies]
# torch = { source = "torch130" }
# torchvision = { source = "torch130" }

# [tool.poetry.dependencies.rootpath]
# git = "https://github.com/hute37/python-rootpath"
# branch = "stable"

  
# ===[BUILD]=================================================================
  
# see: https://packaging.python.org/en/latest/tutorials/packaging-projects/#choosing-a-build-backend
  
# --- [hatch] ---------------------------------------------------

[tool.hatch.metadata]
allow-direct-references = true


[tool.hatch.build.targets.sdist]
include = [
  "src",
  "/tests/pytest",
]

exclude = [
  "*.tar",
  "*.tar.gz",
  ".cache",
  ".cache/uv",
  "images",
  "node_modules",
]

[tool.hatch.build.targets.wheel]
packages = ["src/dve", "src/vce"]
artifacts = [
  "*.so",
  "!/node_modules/*.so",
]

# --- [uv_build] ---------------------------------------------------

# [tool.uv.build-backend]
# namespace = true
# #module-name = ["vce", "dve"]


# --- [setuptools] ---------------------------------------------------

# [tool.setuptools.packages.find]
# where   = ["src"]
# include = ["*"]

# [tool.setuptools]
# include-package-data = true

# [tool.setuptools.package-data]
# res = ["resources/*", "resources/*/*"]


# ===[TEST]=================================================================

# --- [pytest] ---------------------------------------------------

[tool.pytest.ini_options]
pythonpath = ["src", "tests/pytest"]
testpaths = ["tests/pytest"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --tb=short"

log_cli = true
log_cli_level = "DEBUG"

# ===[LINT]=================================================================
  
# --- [mypy] ---------------------------------------------------

[tool.mypy]
python_version = "3.10"
strict = true
ignore_missing_imports = true

# --- [pyright] ---------------------------------------------------

[tool.pyright]
include = ["src", "tests/pytest", "notebooks"]
exclude = [
  "notebooks/@databricks/*",
  "doc/*",
  "home/*",
  "notes/*",
  "renv/*",
  ".venv/*",
  ".agents/*",
  ".eggs/*.py",
  "**/node_modules",
  "**/__pycache__",
  "docker",
  "notes",
  "data",
  "logs",
  "temp",
]
ignore = ["build"]
defineConstant = { DEBUG = true }
typeCheckingMode = "basic"
#typeCheckingMode = "strict"
reportMissingImports = true
#reportMissingTypeStubs = true

# --- [ruff] ---------------------------------------------------

[tool.ruff]
line-length = 100
target-version = "py312"
fix = true

[tool.ruff.lint]
unfixable = []
#ignore-init-module-imports = false
typing-modules = ["pandas._typing"]
exclude = [
  "notebooks/@databricks/*",
  "doc/*",
  ".eggs/*.py",
  "home/*",
  "notes/*",
  "renv/*",
  ".venv/*",
  ".agents/*",
]
select = [
  "F",
  "E",
  "W",
  "YTT",
  "B",
  "Q",
  "T10",
  "INT",
  "PL",
  "PT",
  "PIE",
  "PYI",
  "TID",
  "ISC",
  "TCH",
  "C4",
  "PGH",
  "RUF",
  "S102",
  "NPY002",
  "PERF",
  "FLY",
  "G",
  "FA",
  "ICN001",
  "SLOT",
  "RSE",
]
ignore = []

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.isort]
combine-as-imports = true
#split-on-trailing-comma  = false


[tool.ruff.format]
docstring-code-format = true
exclude = [
  "notebooks/@databricks/*",
  "doc/*",
  ".eggs/*.py",
  "home/*",
  "notes/*",
  "renv/*",
  ".venv/*",
  ".agents/*",
]

# }}} //
# {{{ ////////////////////////////////////////////////////////////////

# ============================================================
# Project Custom Configuration
# ============================================================

[tool.local]
has_config = true
config = "config.yaml"

[tool.local.paths]
log_dir = "logs"

[tool.local.log]
level = "INFO"

[tool.local.hello]
salutation = "Hi"


# }}} //
# {{{ // %*
# LocalWords:  GitLab CommonMark GFM GLFM YAML
# vim: set foldmethod=marker :
# }}} //
