---
description: Analyze `AGENTS.md` proposing enhancements and skill refactoring,, preserving `Claude Code` compatibiliy
---

# AGENT.md optimization

As an expert in "Claude Code" and "Google Antigravity" configuration.

Proposed an optimized version of the following `.agents/AGENTS.md`,
ensuring maximal interoperability between both environments, given that
`.claude/CLAUDE.md`already include this file as mention.
If appropriate, consider refactoring the body in one or more `SKILLS`,
automatically triggered, with skill name prefix `vce-` (e.g. `vce-project-scripts`).

Provide the answer as separated files.
Provide also a clear evaluation and motivate your refactoring.
