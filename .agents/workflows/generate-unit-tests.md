---
description: 
---

* Generate unit tests for each python file and each method
* Make sure the unit tests are named similar to files but with test_ prefix
* Python tests use`pytest`and are placed under `tests/pytest` directory, with a parallel package structure as the conde under test